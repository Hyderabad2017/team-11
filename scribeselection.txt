algorithm for scribe selection

//1.preference of the student
2. qualification
3.location
4.language
5.availability
6.gender
 
/*notify the preference of the student*/
if(username==pref_student)
{
notify.scr_username
}
if(scr_qualificationid<examid)
{
  if(scr_location==student_location)
  { 
    if((student_lang1==scr_lang1)||(student_lang2==scr_lang2))
      {
       if(scr_availability==student_examdate)
        {
         if((student_genpref==scr_gen)||(student_genpref!=NULL))
          {
        notify.scr_username
         }
        }
    }
}
      
       